::program main

imports ::{
    "florence.org/packages/http"
}

struct Endpoint {
    string  resourceName
    int     port
}

main :: () {
    http := http.Serve("/route", routeHandler)
}

routeHandler :: (Response resp, Request @req) {
    resp.Write("<html></html>")
}